# Enforce LF line endings for all text files
* text=auto

# Treat Markdown files are text with LF line endings
*.md text eol=lf

# Treat binary files as binary
*.png binary
*.jpg binary
*.pdf binary

echo "* text=auto" > .gitattributes
